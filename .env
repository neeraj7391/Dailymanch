# MongoDB URI for connecting to the database
MONGO_URI=mongodb+srv://dailymanch69:bc30p38DY2cduN4Q@cluster0.x28aj.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret for signing tokens (make sure to change this in production)
JWT_SECRET=your_jwt_secret_key_here

# Server port (default is 5000 if not specified)
PORT=5000

# Email Configuration (if you're using email sending, e.g., for account verification)
EMAIL_HOST=smtp.your-email-provider.com
EMAIL_PORT=587
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-email-password

# Environment setting (use "development" or "production" depending on your environment)
NODE_ENV=development

# Optionally, you can add any other secrets or configurations needed for APIs, services, etc.
